<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Admin Dashboard | BizBook</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
  <style>
    body {
      margin: 0;
      font-family: 'Segoe UI', sans-serif;
      background-color: #f0f2f5;
    }

    header {
      background-color: #2c3e50;
      padding: 20px 40px;
      color: white;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    header h1 {
      margin: 0;
      font-size: 26px;
    }

    .logout {
      color: #e74c3c;
      text-decoration: none;
      font-weight: bold;
      transition: color 0.3s;
    }

    .logout:hover {
      color: #c0392b;
    }

    .dashboard {
      padding: 40px;
    }

    .stats {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 25px;
      margin-bottom: 40px;
    }

    .card {
      background-color: white;
      padding: 25px;
      border-radius: 12px;
      box-shadow: 0 6px 15px rgba(0, 0, 0, 0.05);
      transition: transform 0.2s ease, box-shadow 0.2s ease;
      text-align: center;
    }

    .card:hover {
      transform: translateY(-5px);
      box-shadow: 0 12px 25px rgba(0, 0, 0, 0.08);
    }

    .card i {
      font-size: 30px;
      color: #3498db;
      margin-bottom: 10px;
    }

    .card h3 {
      margin: 5px 0;
      font-size: 18px;
      color: #34495e;
    }

    .card p {
      font-size: 26px;
      font-weight: bold;
      color: #2c3e50;
    }

    nav {
      display: flex;
      gap: 20px;
      flex-wrap: wrap;
      padding: 20px;
      background-color: #fff;
      border-top: 1px solid #ddd;
      border-bottom: 1px solid #ddd;
    }

    nav a {
      text-decoration: none;
      color: #3498db;
      font-weight: 600;
      display: flex;
      align-items: center;
      gap: 8px;
      transition: all 0.2s ease-in-out;
    }

    nav a:hover {
      color: #1d6fa5;
      transform: scale(1.05);
    }
  </style>
</head>
<body>

  <header>
    <h1>🛠️ BizBook Admin Dashboard</h1>
    <a href="/admin/logout" class="logout"><i class="fas fa-sign-out-alt"></i> Logout</a>
  </header>

  <div class="dashboard">
    <div class="stats">
      <div class="card">
        <i class="fas fa-users"></i>
        <h3>Total Users</h3>
        <p><%= totalUsers %></p>
      </div>
      <div class="card">
        <i class="fas fa-store-alt"></i>
        <h3>Total Businesses</h3>
        <p><%= totalBusinesses %></p>
      </div>
      <div class="card">
        <i class="fas fa-box-open"></i>
        <h3>Total Products</h3>
        <p><%= totalProducts %></p>
      </div>
      <div class="card">
        <i class="fas fa-calendar-check"></i>
        <h3>Total Bookings</h3>
        <p><%= totalBookings %></p>
      </div>
      <div class="card">
        <i class="fas fa-chair"></i>
        <h3>Total Table Bookings</h3>
        <p><%= totalTableBookings %></p>
      </div>
    </div>

    <nav>
      <a href="/admin/users"><i class="fas fa-user"></i> Manage Users</a>
      <a href="/admin/businesses"><i class="fas fa-store"></i> Manage Businesses</a>
      <a href="/admin/products"><i class="fas fa-box"></i> Manage Products</a>
      <a href="/admin/bookings"><i class="fas fa-calendar-alt"></i> Manage Bookings</a>
      <a href="/admin/bookings/tables"><i class="fas fa-chair"></i> Manage Table Bookings</a>
    </nav>

  </div>

</body>
</html